<!DOCTYPE html>
<html dir="<%= t('direction') %>" lang="<%= lang %>">

<head>
    <%- include('../components/metadata_info')%>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/codemirror.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/theme/dracula.min.css">

    <!-- Include CodeMirror JavaScript library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/codemirror.min.js"></script>

    <!-- Include the JavaScript mode for syntax highlighting -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/mode/javascript/javascript.min.js"></script>
    <style>
        html,
        body {
            height: 100%;
            width: 100%;
            overflow: hidden;
            margin: 0%;
        }

        .PAGE {
            width: 100%;
            height: calc(100% - 80px);
            display: grid;
            grid-template-columns: 25% 75%;
        }

        .BODYCONTENT {
            width: calc(100% - 60px);
            height: auto;
            padding: 30px;
            font-family: "Arial";
            color: #1a2028;
        }

        .BODY {
            width: 100%;
            height: 100%;
            overflow-x: hidden;
            overflow-y: scroll;
        }

        .BODYCONTENT h1 {
            color: #fdc535;
        }

        .BODYCONTENT h3 {
            color: #445368;
        }

        #SIDEMENUBUTTON-6 {
            background-color: #684444;
            color: white;
        }
    </style>
</head>

<body>
    <%- include('../components/header') %>

    <div class="PAGE">
            <%- include('../components/htmlTutorialsSideMenu') %>
        <div class="BODY">
            <div class="BODYCONTENT">
                <h1><%= t('htmlTutorials.chapter2.page5.title1') %></h1>
                <p><%= t('htmlTutorials.chapter2.page5.paragraph1') %></p>

                <hr>

                <h1><%= t('htmlTutorials.chapter2.page5.title2') %></h1>
                <p><%= t('htmlTutorials.chapter2.page5.paragraph2') %></p>
                <ul>
                    <li><%= t('htmlTutorials.chapter2.page5.list1Item1') %></li>
                    <li><%= t('htmlTutorials.chapter2.page5.list1Item2') %></li>
                    <li><%= t('htmlTutorials.chapter2.page5.list1Item3') %></li>
                </ul>

                <hr>

                <h1><%= t('htmlTutorials.chapter2.page5.title3') %></h1>
                <p><%= t('htmlTutorials.chapter2.page5.paragraph3') %></p>

                <h3><%= t('htmlTutorials.chapter2.page5.codeTitle1') %></h3>
                <textarea id="CODE1">
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Character Encoding Example</title>
</head>
<body>
    <h1>Welcome to My Webpage</h1>
    <p>This page uses UTF-8 to display special characters like café and 😊.</p>
</body>
</html>
                </textarea>

                <hr>

                <h1><%= t('htmlTutorials.chapter2.page5.title4') %></h1>
                <p><%= t('htmlTutorials.chapter2.page5.paragraph4') %></p>
                <h3><%= t('htmlTutorials.chapter2.page5.codeTitle2') %></h3>
                <textarea id="CODE2">
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Special Symbols Page</title>
</head>
<body>
    <h1>Explore Special Symbols</h1>
    <p>UTF-8 enables rendering of symbols like &copy; (©), &euro; (€), and &hearts; (♥).</p>
</body>
</html>
                </textarea>

                <hr>

                <h2><%= t('htmlTutorials.chapter2.page5.title5') %></h2>
                <p><%= t('htmlTutorials.chapter2.page5.paragraph5') %></p>
                <ul>
                    <li><%= t('htmlTutorials.chapter2.page5.list2Item1') %></li>
                    <li><%= t('htmlTutorials.chapter2.page5.list2Item2') %></li>
                    <li><%= t('htmlTutorials.chapter2.page5.list2Item3') %></li>
                </ul>

                <hr>

                <h2><%= t('htmlTutorials.chapter2.page5.title6') %></h2>
                <p><%= t('htmlTutorials.chapter2.page5.paragraph6') %></p>
                <ul>
                    <li><%= t('htmlTutorials.chapter2.page5.list3Item1') %></li>
                    <li><%= t('htmlTutorials.chapter2.page5.list3Item2') %></li>
                </ul>
            </div>

            <%- include('../components/htmlTutorialsCheckbar', { page_link: 'charmeta' }) %>

            <%- include('../components/footer') %>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.8.0/highlight.min.js"></script>
    <script>
        function getCookie(name) {
            const cookiePattern = `${name}=`;
            const cookies = document.cookie.split('; ');
            for (let cookie of cookies) {
                if (cookie.startsWith(cookiePattern)) {
                    return decodeURIComponent(cookie.substring(cookiePattern.length));
                }
            }
            return null;
        }

        function deleteCookie(cookieName) {
            document.cookie = `${cookieName}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;`;
        }

        var editor1 = CodeMirror.fromTextArea(document.getElementById("CODE1"), {
            lineNumbers: true,
            mode: "javascript",
            theme: "dracula",
            matchBrackets: true,
            autoCloseBrackets: true,
            lineWrapping: true,
            readOnly: true,
        });
        editor1.setSize(null, 250);

        var editor2 = CodeMirror.fromTextArea(document.getElementById("CODE2"), {
            lineNumbers: true,
            mode: "javascript",
            theme: "dracula",
            matchBrackets: true,
            autoCloseBrackets: true,
            lineWrapping: true,
            readOnly: true,
        });
        editor2.setSize(null, 250);
            
    </script>
</body>

</html>