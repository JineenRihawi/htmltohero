<!DOCTYPE html>
<html dir="<%= t('direction') %>" lang="<%= lang %>">

<head>
    <%- include('components/metadata_info')%>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/codemirror.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/theme/dracula.min.css">

    <!-- Include CodeMirror JavaScript library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/codemirror.min.js"></script>

    <!-- Include the JavaScript mode for syntax highlighting -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/mode/javascript/javascript.min.js"></script>
    <style>
        html,
        body {
            height: 100%;
            width: 100%;
            overflow: hidden;
            margin: 0%;
        }

        .PAGE {
            width: 100%;
            height: calc(100% - 80px);
            /* 100% - 2*padding */
            display: grid;
            grid-template-columns: 25% 75%;
        }

        .BODYCONTENT {
            width: calc(100% - 60px);
            /* 100% - 2*padding */
            height: auto;
            padding: 30px;
            font-family: "Arial";
            color: #1a2028;
        }

        .BODY {
            width: 100%;
            height: 100%;
            overflow-x: hidden;
            overflow-y: scroll;
        }

        .BODYCONTENT h1 {
            color: #fdc535;
        }

        .CHECKBAR {
            background-color: #445368;
            margin-bottom: 10px;
            width: calc(100% - 6px);
            padding: 3px;
            display: flex;
            flex-direction: row-reverse;
            user-select: none;
            font-family: "Arial";
            color: white;
        }

        #SIDEMENUBUTTON-1 {
            background-color: #684444;
            color: white;
        }
    </style>
</head>

<body>
    <%- include('components/header') %>

    <div class="PAGE">
        <%- include('components/cssTutorialsSideMenu') %>
        <div class="BODY">
            <div class="BODYCONTENT">
                <h1><%= t('cssTutorials.chapter1.page1.title1') %></h1>
                <p><%= t('cssTutorials.chapter1.page1.paragraph1') %></p>
                <p><%= t('cssTutorials.chapter1.page1.paragraph2') %></p>
                <hr>
                
                <h1><%= t('cssTutorials.chapter1.page1.title2') %></h1>
                <p><%= t('cssTutorials.chapter1.page1.paragraph3') %></p>
                <h3><%= t('cssTutorials.chapter1.page1.codeTitle1') %></h3>
                <textarea id="CODE1">
p {
  color: blue;
  font-size: 16px;
}
                </textarea>

                <h3><%= t('cssTutorials.chapter1.page1.smallTitle1') %></h3>
                <p><%= t('cssTutorials.chapter1.page1.paragraph4') %></p>
                <p><%= t('cssTutorials.chapter1.page1.paragraph5') %></p>
                <hr>

                <h1><%= t('cssTutorials.chapter1.page1.title3') %></h1>
                <ol>
                    <li><%= t('cssTutorials.chapter1.page1.list1Item1') %></li>
                    <li><%= t('cssTutorials.chapter1.page1.list1Item2') %></li>
                    <li><%= t('cssTutorials.chapter1.page1.list1Item3') %></li>
                </ol>

                <h3><%= t('cssTutorials.chapter1.page1.codeTitle2') %></h3>
                <textarea id="CODE2">
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>CSS Example</title>
        <style>
            body {
                background-color: lightblue;
                font-family: Arial, sans-serif;
            }
            h1 {
                color: darkblue;
                text-align: center;
            }
            p {
                color: darkgray;
                font-size: 18px;
            }
        </style>
    </head>
    <body>
        <h1>Welcome to the World of CSS!</h1>
        <p>CSS allows you to style your web pages easily and efficiently.</p>
    </body>
</html>
                </textarea>

                <h3><%= t('cssTutorials.chapter1.page1.listTitle2') %></h3>
                <ol>
                    <li><%= t('cssTutorials.chapter1.page1.list2Item1') %></li>
                    <li><%= t('cssTutorials.chapter1.page1.list2Item2') %></li>
                    <li><%= t('cssTutorials.chapter1.page1.list2Item3') %></li>
                </ol>
            </div>

            <%- include('components/cssTutorialsCheckbar', { page_link: 'tutorialsCss' }) %>

            <%- include('components/footer') %>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.8.0/highlight.min.js"></script>
    <script>
        function getCookie(name) {
            const cookiePattern = `${name}=`;
            const cookies = document.cookie.split('; ');
            for (let cookie of cookies) {
                if (cookie.startsWith(cookiePattern)) {
                    return decodeURIComponent(cookie.substring(cookiePattern.length));
                }
            }
            return null;
        }

        function deleteCookie(cookieName) {
            document.cookie = `${cookieName}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;`;
        }

        var editor = CodeMirror.fromTextArea(document.getElementById("CODE1"), {
            lineNumbers: true,
            mode: "javascript",
            theme: "dracula",
            matchBrackets: true,
            autoCloseBrackets: true,
            lineWrapping: true,
            readOnly: true,
        });
        editor.setSize(null, 100);

        var editor2 = CodeMirror.fromTextArea(document.getElementById("CODE2"), {
            lineNumbers: true,
            mode: "javascript",
            theme: "dracula",
            matchBrackets: true,
            autoCloseBrackets: true,
            lineWrapping: true,
            readOnly: true,
        });
        editor2.setSize(null, 420);

    </script>
</body>

</html>