<!DOCTYPE html>
<html dir="<%= t('direction') %>" lang="<%= lang %>">

<head>
    <%- include('../components/metadata_info')%>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/codemirror.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/theme/dracula.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/codemirror.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.10/mode/javascript/javascript.min.js"></script>
    <style>
        html, body {
            height: 100%;
            width: 100%;
            overflow: hidden;
            margin: 0;
        }

        .PAGE {
            width: 100%;
            height: calc(100% - 80px);
            display: grid;
            grid-template-columns: 25% 75%;
        }

        .BODYCONTENT {
            width: calc(100% - 60px);
            height: auto;
            padding: 30px;
            font-family: "Arial";
            color: #1a2028;
        }

        .BODY {
            width: 100%;
            height: 100%;
            overflow-x: hidden;
            overflow-y: scroll;
        }

        .BODYCONTENT h1 {
            color: #fdc535;
        }

        #SIDEMENUBUTTON-2 {
            background-color: #684444;
            color: white;
        }
    </style>
</head>

<body>
    <%- include('../components/header') %>

    <div class="PAGE">
        <%- include('../components/cssTutorialsSideMenu') %>
        <div class="BODY">
            <div class="BODYCONTENT">
                <h1><%= t('cssTutorials.chapter1.page2.title1') %></h1>
                <p><%= t('cssTutorials.chapter1.page2.paragraph1') %></p>
                <p><%= t('cssTutorials.chapter1.page2.paragraph2') %></p>
                <hr>
                
                <h1><%= t('cssTutorials.chapter1.page2.title2') %></h1>
                <p><%= t('cssTutorials.chapter1.page2.paragraph3') %></p>
                <h3><%= t('cssTutorials.chapter1.page2.codeTitle1') %></h3>
                <textarea id="CODE1">
<p style="color: blue; font-size: 16px;">This is a paragraph with inline CSS.</p>
                </textarea>

                <h3><%= t('cssTutorials.chapter1.page2.smallTitle1') %></h3>
                <p><%= t('cssTutorials.chapter1.page2.paragraph4') %></p>
                <hr>

                <h1><%= t('cssTutorials.chapter1.page2.title3') %></h1>
                <p><%= t('cssTutorials.chapter1.page2.paragraph5') %></p>
                <h3><%= t('cssTutorials.chapter1.page2.codeTitle2') %></h3>
                <textarea id="CODE2">
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Separated CSS</title>
    <style>
        h1 {
            color: darkblue;
            text-align: center;
        }
        p {
            color: darkgray;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <h1>Welcome to CSS!</h1>
    <p>This page uses CSS in a style tag.</p>
</body>
</html>
                </textarea>
            </div>

            <%- include('../components/cssTutorialsCheckbar', { page_link: 'css_use' }) %>

            <%- include('../components/footer') %>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.8.0/highlight.min.js"></script>
    <script>
        function getCookie(name) {
            const cookiePattern = `${name}=`;
            const cookies = document.cookie.split('; ');
            for (let cookie of cookies) {
                if (cookie.startsWith(cookiePattern)) {
                    return decodeURIComponent(cookie.substring(cookiePattern.length));
                }
            }
            return null;
        }

        function deleteCookie(cookieName) {
            document.cookie = `${cookieName}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;`;
        }

        var editor = CodeMirror.fromTextArea(document.getElementById("CODE1"), {
            lineNumbers: true,
            mode: "javascript",
            theme: "dracula",
            matchBrackets: true,
            autoCloseBrackets: true,
            lineWrapping: true,
            readOnly: true,
        });
        editor.setSize(null, 50);

        var editor2 = CodeMirror.fromTextArea(document.getElementById("CODE2"), {
            lineNumbers: true,
            mode: "javascript",
            theme: "dracula",
            matchBrackets: true,
            autoCloseBrackets: true,
            lineWrapping: true,
            readOnly: true,
        });
        editor2.setSize(null, 370);
    </script>
</body>

</html>